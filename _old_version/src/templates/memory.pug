aside
  section
    #title(translate="MEMORY")
    article
      textarea#editor
  section
    #title(translate="VARIABLES")
    article
      table
        tr(ng-repeat='(key, val) in ctrl.sim.variables track by key | orderBy: key')
          td(ng-bind='key')
          td(ng-class='{focus: ctrl.sim.focus.var == key}')
            input(
              type='number', placeholder='0',
              ng-model='ctrl.sim.variables[key]'
            )
        tr
          td(colspan="2", ng-click="ctrl.addTvar()")
            i.fa.fa-plus
